const Spinner = () => {
    return (
        <div role="loading-spinner" className="absolute w-5 h-5 top-[60%] rounded-full spinner border-[0.18em] border-slate-100 animate-spin">
        </div>
    )
}

export default Spinner
